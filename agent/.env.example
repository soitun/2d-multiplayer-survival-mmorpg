# SpacetimeDB connection
SPACETIMEDB_URI=ws://localhost:3000
SPACETIMEDB_MODULE=broth-bullets-local

# API Proxy (Railway) for LLM calls
PROXY_URL=http://localhost:8002

# NPC Configuration
NPC_COUNT=20
PLANNER_INTERVAL_MS=30000
FAST_LOOP_HZ=10
