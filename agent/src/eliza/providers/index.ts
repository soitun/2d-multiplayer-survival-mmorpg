/**
 * ElizaOS Provider re-exports.
 * Providers supply structured data to the agent context — never prose.
 */

export {
  selfStateProvider,
  worldStateProvider,
  threatsProvider,
  tasksProvider,
  chatContextProvider,
} from '../plugin.js';
