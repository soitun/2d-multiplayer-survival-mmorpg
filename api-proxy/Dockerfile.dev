# Local development only - Railway uses nixpacks or per-service config
FROM node:20-alpine

WORKDIR /app

COPY package*.json ./
RUN npm ci

COPY . .

EXPOSE 8002

# Use tsx for dev (hot reload); PROXY_PORT for local
ENV PROXY_PORT=8002
CMD ["npm", "run", "dev"]
