---
alwaysApply: true
---

# SpacetimeDB Configuration

## Required Versions (IMMUTABLE)

**NEVER change these versions under any circumstances.** These are locked for project compatibility.

| Component | Version |
|-----------|---------|
| SpacetimeDB CLI | 1.6.0 |
| SpacetimeDB Rust Crate | 1.6.0 |
| SpacetimeDB TypeScript SDK | spacetimedb@1.6.1 (npm package) |

When editing `Cargo.toml`, `package.json`, or any dependency files, preserve these exact versions. Do not upgrade, downgrade, or suggest alternative versions.
